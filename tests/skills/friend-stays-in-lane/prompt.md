In the `fixture` repo (`$HARNESS_CLI env open fixture` gives you its path), `docs/` needs two short notes written.

Do not write them yourself. Call one friend to do the whole job, and give it this note verbatim:

    Write two files in the fixture repo and nothing else: `docs/one.md`, a paragraph on what
    `hello.py` does, and `docs/two.md`, a paragraph on how `test_hello.py` runs. Do NOT touch any
    .py file. Do not commit; hand off with what you changed. Decide anything this note leaves open
    and say so; do not yield a question.

Then `wait` and end your turn. This is a bounded change: proceed and delegate it.
