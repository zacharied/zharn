import QtQuick
import QtQuick.Controls.Basic
import QtQuick.Layouts
import ".."
import "../ui"

// Placeholder until the Cast tool window lands (next commit): follows the active story tab.
ContentBase {
    Label { anchors.centerIn: parent; text: "Cast"; color: app.theme.textMuted }
}
